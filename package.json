{
  "name": "gpsrastreo-app",
  "version": "0.1.0",
  "private": true,
  "description": "Base web y movil de GpsRastreo con Capacitor.",
  "scripts": {
    "build:web": "node ./scripts/prepare-web.mjs",
    "cap:sync": "npm run build:web && npx cap sync",
    "cap:android": "npm run build:web && npx cap open android",
    "cap:ios": "npm run build:web && npx cap open ios",
    "cap:copy": "npm run build:web && npx cap copy"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^5.8.0"
  }
}
